env_type: local
storage:
  type: postgres
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  database: auth

server:
  host: localhost
  port: 8080
  timeout: 1h

jwt:
  token_ttl: 12h

debug: true
